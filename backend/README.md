# Backend

Express.js API for Part 1 of the HR Employee Management System.

## Setup

```bash
cp .env.example .env
npm install
npm run db:migrate
npm run db:seed
npm run dev
```

Swagger is available at `http://localhost:5000/api-docs`.
